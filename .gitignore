__pycache__/
*.pyc
.venv/
.env

# The generated site. Every page is rebuilt from the database in a few
# minutes, so it is output, not source — 2,484 files that used to be
# committed and rewritten every quarter of an hour. What is worth keeping is
# the database, and that is what the backups hold (ops/backup/pull-backup.sh).
site/
# Likewise the machine-readable catalog export: regenerated by every build.
data/catalog.json
